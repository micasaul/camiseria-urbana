'use strict';

/**
 * detalle-reserva controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::detalle-reserva.detalle-reserva');
